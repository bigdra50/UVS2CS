using UnityEngine;

public class Obstacle_Behavior_ER : MonoBehaviour
{
    private void OnTriggerEnter2D()
    {
        null.position = .ctor(15f, Range(minHeight, maxHeight), 0f);
    }

    private void FixedUpdate()
    {
        null.velocity = .ctor(speed, 0f);
    }
}
