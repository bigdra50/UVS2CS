using UnityEngine;

public class Obstacle_Behavior_ER : MonoBehaviour
{
    private void OnTriggerEnter2D()
    {
        null.position = null..ctor(0f, null.Range(minHeight, maxHeight), 0f);
    }

    private void FixedUpdate()
    {
        null.velocity = null..ctor(speed, 0f);
    }
}
