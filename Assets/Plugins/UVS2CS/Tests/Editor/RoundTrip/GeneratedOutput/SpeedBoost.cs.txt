using UnityEngine;

public class SpeedBoost : MonoBehaviour
{
    private void FixedUpdate()
    {
        if (String)
        {
            if (Single)
            {
            }
        }
    }

    private void OnTriggerEnter()
    {
    }
}
