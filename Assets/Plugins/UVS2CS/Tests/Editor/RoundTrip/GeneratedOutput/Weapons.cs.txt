using UnityEngine;

public class Weapons : MonoBehaviour
{
    private object currentBullet;

    private void OnFire()
    {
        if (terraformerAnimator.GetBool(""))
        {
            muzzleVFX.Play();
            null.GetPooledObject();
            if (null.GetPooledObject() != null)
            {
                null.SetTrigger("");
                currentBullet = null.GetPooledObject();
                currentBullet.SetPositionAndRotation(bulletSpawnPoint.position);
                currentBullet.SetActive(false);
            }
            null.SetTrigger("");
            currentBullet = null.GetPooledObject();
            currentBullet.SetPositionAndRotation(bulletSpawnPoint.position);
            currentBullet.SetActive(false);
        }
        muzzleVFX.Play();
        null.GetPooledObject();
        if (null.GetPooledObject() != null)
        {
            null.SetTrigger("");
            currentBullet = null.GetPooledObject();
            currentBullet.SetPositionAndRotation(bulletSpawnPoint.position);
            currentBullet.SetActive(false);
        }
        null.SetTrigger("");
        currentBullet = null.GetPooledObject();
        currentBullet.SetPositionAndRotation(bulletSpawnPoint.position);
        currentBullet.SetActive(false);
    }

    private void Start()
    {
        muzzleFlash.Stop();
    }
}
