using UnityEngine;

public class SpaceshipBehaviors_TutorialBase : MonoBehaviour
{
    private void FixedUpdate()
    {
        if (Single)
        {
        }
    }

    private void FixedUpdate_1()
    {
        String.rotation = Single;
    }
}
